package com.safetynet.SafetyNetAlert.services;

public class JsonOutputFactory {
/*

    public JsonOutputService createJsonOutputService(String jsonUrl, String variable) {
        JsonOutputService service = null;

        switch (jsonUrl) {
            case "phoneAlert":
                service = new JsonOutputServicePhoneAlert(Integer.parseInt(variable.trim()));
                break;
            case "firestation":
                service = new JsonOutputServiceFirestation(Integer.parseInt(variable.trim()));
                break;/*
            case "de":
                service = new HelloWorldServiceGermanImpl();
                break;
            case "jp":
                service = new HelloWorldServiceJapanImpl();
                break;
            default:
                service = new HelloWorldServiceEnglishImpl();
        }
        return service;
    }*/
}
